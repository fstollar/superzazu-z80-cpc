name: tests runner

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make
    - name: testing
      run: ./z80_tests
    - name: Verify CPC timing table
      run: python3 tools/verify_cpc_timing.py
